# 开发进度

## 项目

- 项目名称：AI合同初审助手微信小程序 V1
- 当前负责人范围：后端主链路开发
- 最近更新时间：2026-05-20

## 当前进度概览

- 已完成后端 V1 工程初始化
- 已完成合同输入到审核结果返回的主链路
- 已完成历史记录查询与删除能力
- 已完成基础异常处理与统一响应结构
- 审核能力当前为 Mock 版本，接口结构已按正式版设计

## 本次已完成

### 1. 后端工程初始化

- 技术栈确定为 `Python + FastAPI + SQLite`
- 已创建后端目录：`backend/`
- 已补充启动说明与依赖文件

### 2. 已实现接口

- `POST /api/v1/contracts/upload`
- `POST /api/v1/contracts/text`
- `GET /api/v1/contracts/{task_id}/preview`
- `POST /api/v1/contracts/{task_id}/audit`
- `GET /api/v1/contracts/{task_id}/result`
- `GET /api/v1/contracts/history`
- `DELETE /api/v1/contracts/{task_id}`
- `GET /health`

### 3. 已实现核心能力

- 文件上传与本地文件存储
- PDF 文本提取
- DOCX 文本提取
- 文本标准化处理
- 审核任务状态流转
- 结构化审核结果输出
- 历史记录存储与返回

### 4. 已实现数据结构

- 审核任务表 `contract_tasks`
- 审核结果表 `audit_results`

### 5. 已覆盖异常流程

- 上传失败
- 文件格式不支持
- 解析失败
- 审核失败
- 结果为空/内容无效

## 当前约束

- `doc` 文件当前仅做明确降级提示，建议转成 `docx` 后上传
- 暂未接入真实大模型审核服务
- 暂未实现 OCR
- 暂未实现用户鉴权体系，当前联调通过请求头 `X-User-Id` 区分用户

## 下一步建议

- 接入真实大模型审核服务
- 输出前后端联调接口样例
- 根据前端联调情况补充结果页字段或错误码

## 备注

- 后续每次开发完成后，都需要同步更新本文件
