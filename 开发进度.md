# 开发进度

## 项目

- 项目名称：AI合同初审助手微信小程序 V1
- 当前负责范围：前后端主链路、联调支持、真实模型接入与生产验证准备
- 最近更新时间：2026-05-20

## 当前阶段结论

- V1 前后端基线已基本冻结
- 前端真实联调地址已稳定为 `http://127.0.0.1:8010`
- 当前阶段已从“人工界面级验收”进入“真实大模型接入与生产验证准备”
- 接口结构继续保持稳定，不扩到 OCR、专项合同审核、企业协作等 V2 范围

## 已完成基础能力

### 后端主链路

- 已完成后端 V1 工程初始化
- 已完成合同输入到审核结果返回的主链路
- 已完成历史记录查询与删除能力
- 已完成统一异常处理与统一响应结构

### 已实现接口

- `POST /api/v1/contracts/upload`
- `POST /api/v1/contracts/text`
- `GET /api/v1/contracts/{task_id}/preview`
- `POST /api/v1/contracts/{task_id}/audit`
- `GET /api/v1/contracts/{task_id}/result`
- `GET /api/v1/contracts/history`
- `DELETE /api/v1/contracts/{task_id}`
- `GET /health`

### 已实现核心能力

- 文件上传与本地文件存储
- PDF 文本提取
- DOCX 文本提取
- 文本标准化处理
- 审核任务状态流转
- 结构化审核结果输出
- 历史记录存储与返回

### 当前数据结构

- 审核任务表 `contract_tasks`
- 审核结果表 `audit_results`

### 已覆盖异常流程

- 上传失败
- 文件格式不支持
- 解析失败
- 审核失败
- 结果为空

## 已完成联调支持

- 已输出 [接口文档.md](D:\HT_project\HT_project\接口文档.md)
- 已确认真实联调地址为 `http://127.0.0.1:8010`
- 已确认 `GET /health` 可访问
- 已完成命令行真实主链路验证
- 已确认中文内容在正确 UTF-8 请求下接口实际返回正常

### 已完成兼容字段补充

- `/result` 同时返回：
  - `result`
  - `summary`
  - `risks`
- `/history` 已补充：
  - `id`
  - `title`
  - `status_text`
  - `completed_at`

## 本轮完成

### 1. 真实模型接入方案落地

- 已确认 mock 替换点为 [audit_service.py](D:\HT_project\HT_project\backend\app\services\audit_service.py)
- 已保持 [result_formatter.py](D:\HT_project\HT_project\backend\app\services\result_formatter.py) 继续作为统一结构化出口
- 已保持 `/result` 与 `/history` 对前端的返回结构不变

### 2. 已接入真实模型审核服务骨架

- 新审核服务采用“真实模型优先，mock 兜底”的方式
- 当环境中配置 `OPENAI_API_KEY` 时，优先调用真实模型
- 当未配置密钥，或真实模型失败且允许兜底时，自动降级到 mock 审核
- 所有审核结果仍统一经过 `normalize_audit_result()` 清洗

### 3. 已增加生产化机制

- 超时控制
- 重试策略
- 失败兜底
- 日志记录
- 输入长度限制

### 4. 已新增生产验证资料

- 已新增 [生产验证清单.md](D:\HT_project\HT_project\生产验证清单.md)
- 已更新 [backend\README.md](D:\HT_project\HT_project\backend\README.md)

## 当前约束

- 当前环境尚未配置 `OPENAI_API_KEY`
- 因此本轮只能完成“真实模型接入代码 + 无密钥降级验证”，还不能完成真实联网审核验证
- `doc` 文件当前仍只做明确降级提示
- OCR 仍不在 V1 范围内
- 当前仍未接入正式用户鉴权体系，联调通过请求头 `X-User-Id`

## 第一轮验证结论

- 已完成“无 API Key 场景”验证
- 验证结果：服务会自动降级到 mock 审核，且接口结构保持稳定
- 当前已进入“生产验证准备阶段”
- 但尚未完成“真实模型实际联网调用验证”，原因是缺少模型密钥配置

## 当前主要风险

- 当前最大的阻塞不是接口，而是运行环境未配置真实模型密钥
- 接入真实模型后，风险重点会从“接口联调”转向：
  - 输出结构稳定性
  - 响应时延
  - 失败重试表现
  - 风险结果质量

## 下一步建议

1. 配置 `OPENAI_API_KEY` 与模型相关环境变量
2. 重新启动 `8010` 后端服务
3. 使用真实合同样例执行第一轮真实模型验证
4. 记录耗时、失败率、结构稳定性与风险质量问题
5. 根据验证结果继续优化审核 prompt、错误码与降级策略

## 备注

- 后续每次开发完成后，继续同步更新本文件
