# 开发进度

## 项目

- 项目名称：AI合同初审助手微信小程序 V1
- 当前负责人范围：前后端主链路开发
- 最近更新时间：2026-05-20

## 当前进度概览

- 已完成后端 V1 工程初始化
- 已完成合同输入到审核结果返回的主链路
- 已完成历史记录查询与删除能力
- 已完成基础异常处理与统一响应结构
- 已完成前端微信小程序 V1 工程初始化
- 已完成前端 7 个核心页面与主链路页面跳转
- 已完成前端本地 Mock 数据串联，可预览“输入 -> 确认 -> 审核中 -> 结果 -> 历史”闭环
- 已完成前端真实接口请求层改造与审核轮询逻辑接入
- 已输出前后端联调用 `接口文档.md`
- 已完成后端联调兼容字段补充
- 已确认真实联调地址并完成主链路命令行自测
- 已确认本机存在微信开发者工具与 CLI
- 已补充前端历史页与结果页对兼容字段的实际消费

## 本次已完成

### 1. 后端工程初始化

- 技术栈确定为 `Python + FastAPI + SQLite`
- 已创建后端目录：`backend/`
- 已补充启动说明与依赖文件

### 2. 已实现接口

- `POST /api/v1/contracts/upload`
- `POST /api/v1/contracts/text`
- `GET /api/v1/contracts/{task_id}/preview`
- `POST /api/v1/contracts/{task_id}/audit`
- `GET /api/v1/contracts/{task_id}/result`
- `GET /api/v1/contracts/history`
- `DELETE /api/v1/contracts/{task_id}`
- `GET /health`

### 3. 已实现核心能力

- 文件上传与本地文件存储
- PDF 文本提取
- DOCX 文本提取
- 文本标准化处理
- 审核任务状态流转
- 结构化审核结果输出
- 历史记录存储与返回

### 4. 已实现数据结构

- 审核任务表 `contract_tasks`
- 审核结果表 `audit_results`

### 5. 已覆盖异常流程

- 上传失败
- 文件格式不支持
- 解析失败
- 审核失败
- 结果为空/内容无效

### 6. 前端工程初始化

- 已创建微信小程序前端目录：`miniprogram/`
- 已补充小程序基础配置：
  - `app.json`
  - `app.js`
  - `app.wxss`
  - `project.config.json`
- 已建立前端共享能力：
  - 本地草稿存储
  - 当前审核任务存储
  - 历史记录存储
  - 风险等级映射与格式化工具
  - Mock API 与 Mock 审核结果

### 7. 已完成前端页面

- 首页
- 上传/粘贴输入页
- 内容确认页
- 审核中页
- 审核结果页
- 历史记录页
- 说明与免责声明页

### 8. 已完成前端交互与状态

- 文件上传与文本输入二选一提交流程
- 文件优先于文本的提示逻辑
- 文本字数统计与空内容禁用
- 内容预览与返回修改
- 审核中加载态与失败重试
- 风险结果按等级排序展示
- 单条风险建议复制
- 历史记录查看与删除
- 结果为空提示
- 关键页面免责声明与使用边界提示

### 9. 前端接口预留情况

- 已按 PRD / 技术方案预留以下前端联调方法：
  - `uploadFile`
  - `submitText`
  - `getPreview`
  - `startAudit`
  - `getAuditResult`
  - `listHistory`
  - `deleteHistory`
- 当前主链路请求已切换为真实 `wx.request` / `wx.uploadFile`

### 10. 本轮新增后端联调产物

- 已新增 [接口文档.md](D:\HT_project\HT_project\接口文档.md)
- 已补充 base URL、请求头、统一成功/失败结构
- 已补充所有核心接口的请求/响应示例
- 已补充 `/result` 在 `analyzing / success / failed` 三种状态下的返回示例
- 已补充上传文件支持类型与 `doc` 降级说明
- 已补充前后端字段命名差异与联调风险提示

### 11. 本轮新增后端兼容处理

- `/result` 接口已额外返回 `summary` 与 `risks` 字段，方便兼容前端原有结果页结构
- `/history` 接口已补充：
  - `id`
  - `title`
  - `status_text`
  - `completed_at`
- `upload` 响应结构已显式支持 `page_count`
- [backend\\README.md](D:\HT_project\HT_project\backend\README.md) 已补充健康检查地址与联调约定

### 12. 本轮新增前端联调改造

- 已将 [api.js](D:\HT_project\HT_project\miniprogram\services\api.js) 从 Mock Promise 改为真实请求封装
- 已统一前端 `baseURL` 为 `http://127.0.0.1:8010`
- 已统一请求头 `X-User-Id: demo-user`
- 已完成真实接口接入：
  - `POST /api/v1/contracts/upload`
  - `POST /api/v1/contracts/text`
  - `GET /api/v1/contracts/{task_id}/preview`
  - `POST /api/v1/contracts/{task_id}/audit`
  - `GET /api/v1/contracts/{task_id}/result`
  - `GET /api/v1/contracts/history`
  - `DELETE /api/v1/contracts/{task_id}`
- 已完成审核中页轮询逻辑：
  - `analyzing` 持续轮询
  - `success` 自动跳转结果页
  - `failed` 展示失败提示
- 已完成历史记录页真实接口读取与删除
- 已完成结果页对真实后端返回结构的适配
- 已在请求层统一处理：
  - `success + data` 解包
  - `snake_case` 到页面结构映射
  - 网络异常与业务异常提示
- 已补充兼容字段消费：
  - 结果页优先消费 `summary + risks`
  - 历史页消费 `title / status_text / completed_at`

### 13. 本轮联调结论

- 前端真实请求层已接通代码层
- 已确认 `http://127.0.0.1:8000` 不是本项目后端，而是其他 FastAPI 服务
- 已确认本项目真实联调地址为 `http://127.0.0.1:8010`
- 已确认 `/health` 可访问并返回成功
- 已完成真实命令行闭环验证：
  - 提交文本
  - 获取预览
  - 发起审核
  - 轮询结果
  - 查看历史
  - 删除历史
- 已完成文件上传接口验证：
  - PDF 上传成功
  - DOCX 上传成功
  - DOC 返回预期降级提示
- 已确认中文内容在正确 UTF-8 请求下接口实际返回正常
- 已确认本机微信开发者工具安装路径：
  - `D:\微信web开发者工具\微信开发者工具.exe`
  - `D:\微信web开发者工具\cli.bat`
- 已尝试使用 CLI 执行 `open / auto / preview / islogin`
- 当前结果：
  - 工具可启动
  - 项目窗口可被打开
  - 但 CLI 调用持续超时，未返回可继续执行界面交互自动化所需的稳定结果
- 因此，当前环境下仍未完成“微信开发者工具内界面点击级”真实自测

### 14. 当前需要后端确认

- 历史记录友好展示字段已由后端补充
- 已确认文本提交后的中文 `?` 主要是终端/脚本编码显示问题，非后端 JSON 固有问题

## 当前约束

- `doc` 文件当前仅做明确降级提示，建议转成 `docx` 后上传
- 暂未接入真实大模型审核服务
- 暂未实现 OCR
- 暂未实现用户鉴权体系，当前联调通过请求头 `X-User-Id` 区分用户
- 当前环境里虽已确认微信开发者工具存在，但未拿到可继续做页面点击自动化的稳定入口，因此“微信开发者工具内的界面点击级真实自测”仍需人工补跑或提供自动化票据/方案

## 当前联调风险点

- 后端统一返回 `success + data` 包裹结构
- 后端字段命名为 `snake_case`，前端已在请求层统一映射
- 后端结果结构已补充 `summary + risks` 兼容字段，命令行验证通过，仍建议在页面交互中复核一次
- 前端请求层已优先消费 `summary + risks` 兼容字段
- 审核流程轮询已用真实接口验证通过
- 文件上传真实接入已切换 `wx.uploadFile`，命令行验证通过，仍建议在微信开发者工具里复核一次
- 中文内容在正确 UTF-8 请求下接口返回正常，但仍需在微信开发者工具界面中复核最终显示
- 微信开发者工具 CLI 当前超时，无法在本环境内继续完成真正的界面点击级自动化验收

## 下一步建议

- 在微信开发者工具中人工完成一次真实主链路自测，或提供可用的自动化票据/自动化方案
- 在前端请求层统一做字段映射，避免页面层直接消费后端原始结构
- 联调稳定后再接入真实大模型审核服务
- 根据前端联调情况补充结果页字段或错误码
- 启动微信开发者工具验收前，确保本地运行的就是当前最新版 `8010` 后端服务

## 备注

- 后续每次开发完成后，都需要同步更新本文件
- 后续前端开发进展也统一记录在本文件中
