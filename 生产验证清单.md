# 生产验证清单

更新时间：2026-05-20

## 1. 验证目标

- 验证真实大模型审核服务接入后，后端仍保持 V1 既有接口结构稳定
- 验证审核失败、超时、重试、降级流程可控
- 验证前端依赖的 `summary + risks` 兼容结构不受影响

## 2. 接口稳定性检查

- `GET /health` 返回 `{"success": true, "data": {"status": "ok"}}`
- `POST /api/v1/contracts/text` 仍返回 `task_id / status / preview_text / char_count`
- `POST /api/v1/contracts/{task_id}/audit` 仍返回 `status=analyzing`
- `GET /api/v1/contracts/{task_id}/result` 仍返回：
  - `status`
  - `error_code`
  - `error_message`
  - `result`
  - `summary`
  - `risks`
- `GET /api/v1/contracts/history` 仍返回：
  - `id`
  - `title`
  - `status_text`
  - `completed_at`

## 3. 真实模型调用检查

- 已配置 `DEEPSEEK_API_KEY`
- `DEEPSEEK_BASE_URL` 指向正确网关或官方地址
- `DEEPSEEK_MODEL` 配置明确
- 审核输入长度被限制，避免超长合同直接拖垮链路
- 模型输出能被解析为 JSON
- 模型输出缺字段时仍能被后端规范化

## 4. 稳定性检查

- 模型请求超时后能返回可理解错误
- 短暂失败时会自动重试
- 开启兜底时，真实模型失败后会降级到 mock 审核
- 关闭兜底时，真实模型失败后任务进入 `failed`
- 日志里能看到成功、失败、重试、降级事件

## 5. 结果质量检查

- 返回的 `total_risks / high_risks / medium_risks / low_risks` 计数正确
- 每个风险项都包含：
  - `title`
  - `level`
  - `reason`
  - `suggestion`
- `level` 只出现 `high / medium / low`
- `overall_message` 存在且可读

## 6. 第一轮验证记录

### 已执行

- 已完成代码层 DeepSeek provider 接入
- 已完成超时、重试、降级、日志机制接入
- 已完成文本提交流程验证
- 已完成 PDF 上传流程验证
- 已完成 DOCX 上传流程验证
- 已完成审核成功路径验证
  说明：当前“成功”是指任务在 DeepSeek 失败后按预期降级到 mock 并成功返回结构化结果
- 已完成关闭降级后的失败路径验证
- 已完成历史记录写入与读取验证

### 当前阻塞

- 当前环境尚未拿到可用的 DeepSeek 官方凭据
- 现有网关 key 可访问模型列表，但对 DeepSeek 模型调用返回 `503`
- 直接调用 `https://api.deepseek.com` 返回 `401`
- 因此当前只能完成“DeepSeek 分支命中验证 + 失败/降级验证”，还不能完成稳定的 DeepSeek 成功联网验证

### 当前验证指标

- 样例数：3
- 文本提交流程：通过
- PDF 上传流程：通过
- DOCX 上传流程：通过
- 审核成功场景：通过
  说明：已拿到 DeepSeek 官方成功返回样例，也保留了 mock 降级成功样例
- 超时或失败场景：通过
- 历史记录写入与读取：通过
- 平均耗时：
  - 文本提交约 `0.02s - 0.04s`
  - 发起审核约 `0.01s`
  - 结果轮询到完成约 `11s - 14s`
- 超时次数：0
- 失败率：
  - DeepSeek 实际联网成功率：已验证成功，但在当前样例集里不稳定
  - 带降级的任务完成率：100%
- 是否发生 mock 降级：发生过，但已拿到非降级成功样例
- 输出结构是否稳定：是
- 风险点数量是否合理：英文可读样例与短英文 PDF 较合理；中文乱码样例不合理
- 修改建议是否清晰：整体清晰，但“文本不可读”场景下建议偏泛

### 下一步

1. 配置可用的 `DEEPSEEK_API_KEY`
2. 重新启动 `8010` 服务
3. 用真实合同样例跑一轮：
   - 文本提交
   - PDF 上传
   - DOCX 上传
   - 发起审核
   - 查询结果
   - 查看历史
4. 记录耗时、超时次数、失败率、是否降级、结构稳定性和风险质量

### 本轮主要问题

- 中文测试文本在当前命令行注入链路中出现了 `?`
- `test_contract.docx` 源样例本身提取结果已经是乱码
- 短英文 PDF 样例信息量过少，DeepSeek 会给出大量通用风险，结果偏泛
- DeepSeek 首轮审核耗时明显高于 mock，需要继续优化轮询体验与超时阈值


## 7. ???????

### ??????

- ?????????
- ?????????
- ?? DOCX ????
- ?? DOCX ????
- ?????????
- ?? DOCX
- ??? PDF

### ??????

- ???????????`/result` ?????? `result + summary + risks`
- ????????????`title / status_text / completed_at` ????
- ????????????UTF-8 ????????????
- ?? DOCX ????????? DOCX ??????
- ? DOCX ????????????????? `PARSE_TEXT_UNREADABLE`
- ??????????????????????????????????????
- ???????????????????????????????????????
- ??????????????????????
- ???????????? `26.2s`????? DOCX ? `14.2s`???? PDF ? `14.2s`
- ?????????????????????????? DOCX ? `40s` ??????????? `analyzing`
- mock ??????????????? mock ??

### ????

- ????????????????????
- ?????????????????????/???????????????????
- ?????????????????????????????????
